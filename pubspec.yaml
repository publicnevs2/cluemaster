name: clue_master
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  path_provider: ^2.1.0
  cupertino_icons: ^1.0.0
  image_picker: ^1.0.0
  uuid: ^4.0.0
  record: ^5.0.0
  just_audio: ^0.9.0
  audio_session: ^0.1.0
  mobile_scanner: ^5.1.1
  video_player: ^2.8.6
  intl: ^0.19.0

  # Pakete für das Teilen
  archive: ^3.4.9
  share_plus: ^7.2.2

  # NEUES PAKET FÜR DEN IMPORT
  file_picker: ^10.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

dependency_overrides:
  record_linux: 1.1.1
  record_platform_interface: 1.3.0

flutter:
  uses-material-design: true
  assets:
    - assets/hunts.json
    - assets/codes.json
    - assets/images/
